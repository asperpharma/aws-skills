interface:
  display_name: "Aws Skills"
  short_description: "Repo-specific patterns and workflows for aws-skills"
  default_prompt: "Use the aws-skills repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true